
export const metadata = {
  title: 'AI Voice Changer',
  description: 'Turn text into voice using AI',
}

export default function RootLayout({ children }) {
  return (
    <html lang='en'>
      <body>{children}</body>
    </html>
  )
}
import './globals.css'
